# =============================================================================
# PRODUCTION ENVIRONMENT — SI PRESENSI PRO MAX
# =============================================================================
# Nilai VITE_SUPABASE_URL dan VITE_SUPABASE_ANON_KEY diisi via GitHub Secrets
# saat build di GitHub Actions. Jangan hardcode di sini!
# Untuk build lokal: copy dari .env (yang ada di .gitignore)

# App
VITE_APP_NAME=SI PRESENSI PRO MAX
VITE_APP_URL=https://rihard-cyber.github.io/Aplikasi_Absensi
